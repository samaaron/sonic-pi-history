defonce :val2 do
  OSCVal.new(8000)
end

use_synth :dsaw

loop do
  v = val2.read("/1/fader2")
  v2 = val2.read("/1/fader1")
    
  play (30 + (v2 * 80)).to_i, release: v, attack: 0
  sleep v + 0.01
end